export interface ContactUsJSON {
    button:string,
    caption: string,
    emailError: string,
    error: string,
    errorSending: string
    fields: string[],
    reasons: string[],
    thanks: string,
    title: string
}